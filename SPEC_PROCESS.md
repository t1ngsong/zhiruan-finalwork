# SPEC_PROCESS.md — 过程文档

> Coding Agent Harness · AI4SE 期末项目 · A 方向

---

## 1. Brainstorming 关键节点

### 1.1 初始方向选择（2026-08-10）

在阅读课程要求后，面临两个方向的选择：

- **A 方向**：Coding Agent Harness（构建 Agent 的基础设施）
- **B 方向**：非 harness 应用类项目

选择 A 方向，理由：
1. 课程核心命题是"Agent = LLM + Harness"，A 方向直接回应这个命题
2. Harness 要求自己编码主循环、工具分发、护栏、反馈闭环——有明确的工程深度
3. Mock LLM 确定性测试是独特的挑战
4. 更贴近课程"理解 AI 系统本质"的教学目标

### 1.2 技术选型决策链

| 决策点 | 选项 | 选择 | 理由 |
|--------|------|------|------|
| 编程语言 | Python / TypeScript / Go | **Python 3.11+** | LLM SDK 生态最成熟，pytest 测试框架丰富 |
| LLM 供应商 | DeepSeek / 通义千问 / 智谱 | **DeepSeek** | 国产、性价比高、代码能力强、兼容 OpenAI SDK |
| 重点维度 | 治理护栏 / 反馈闭环 / 扩展 | **治理护栏** | 核心逻辑完全确定性（正则匹配+风险评分+状态机），与 mock 单测天然契合 |
| 配置格式 | YAML / TOML / JSON | **YAML** | 人类可读写，Python 生态成熟 |
| 凭据加密 | AES-256-GCM + PBKDF2 | **cryptography** | 工业标准 |
| 分发方式 | Docker / pip / 二进制 | **Docker** | 一键运行、无 Python 环境依赖 |
| CLI 框架 | argparse / click / typer | **argparse** | Python 标准库、零依赖 |
| 前端 | 无（纯 CLI） | **豁免** | 参照 Open Design 要求豁免 |

### 1.3 工具系统设计决策

课程要求"至少 3 个工具"，讨论后确定 4 个：

| 工具 | 风险 | 理由 |
|------|------|------|
| `read_file` | LOW | 只读操作，workspace 内安全 |
| `write_file` | MEDIUM | 修改文件，需范围围栏 |
| `shell` | 动态评分 | 风险最高，需模式匹配+审批 |
| `search` | LOW | 只读操作，grep 语义安全 |

反馈信号选择全部 4 种：退出码、测试结果、lint 问题、类型错误。

记忆系统选择最低实现：项目约定（`.agent/rules.yaml`）+ 历史决策（`.agent/decisions.jsonl`）。

## 2. 三轮关键迭代

### 第一轮：架构设计迭代（2026-08-10）

**初始方案**：简单的 "LLM → 工具 → 反馈" 循环，所有组件耦合在一起。

**发现的问题**：通过自检发现架构不够清晰，组件间耦合度高，mock 测试困难。

**修正**：
- 引入顺序管道架构：CLI → 配置 → 上下文 → LLM → 解析 → 护栏 → 工具 → 反馈 → 停机
- 每层独立，可独立单测
- LLM 抽象层使用 ABC + MockAdapter + DeepSeekAdapter 三实现
- 护栏采用四层防护：模式匹配 → 风险评分 → HITL 审批 → 范围围栏

### 第二轮：护栏深度设计迭代（2026-08-10）

**初始方案**：简单的正则匹配 + 黑白名单。

**发现的问题**：通过头脑风暴发现护栏应该有层次——不是所有"危险"都应该直接拒绝，也不是所有"安全"都应该放行。

**修正**：
- 引入四级风险等级：LOW / MEDIUM / HIGH / FATAL
- FATAL 直接拒绝（`rm -rf /`、`DROP TABLE`）
- MEDIUM/HIGH 触发 HITL 审批（60 秒超时）
- LOW 自动放行
- 增加范围围栏防止工作区外的路径访问
- HITL 状态机：IDLE → WAITING → (APPROVED / REJECTED / TIMEOUT) → IDLE

### 第三轮：SPEC 自检与修正（2026-08-10）

**发现的问题**：
1. SPEC 中工具数量不一致（§3.2 写 4 个，§9 和 §11.2 写"3个"）
2. 文件操作路径穿越防护设计不足
3. 凭据管理缺少"清除"功能
4. 配置优先级未明确

**修正**：
1. 统一工具数量为"4个"
2. 增加路径穿越防护设计（`is_relative_to`）
3. 增加 `setup --clear` 命令
4. 明确 CLI 参数 > 配置文件 > 默认值 的优先级链

## 3. 冷启动验证记录

### 验证方法

按照课程要求，用不同的 Agent（Codex CLI，与主开发用的 Claude Code 不同）仅凭 SPEC + PLAN 试实现 1-2 个 task。

### 试实现 Task：Action Parser（动作解析器）

**输入**：仅提供 SPEC.md 和 PLAN.md，不提供任何其他上下文。

**结果**：
- ✅ 成功创建 `src/agent/parser.py` 和 `tests/test_parser.py`
- ✅ 实现了 TEXT / TOOL_CALL / FINISH 三种类型的解析
- ✅ 测试覆盖正常路径和 JSON 解析失败边界
- ⚠️ FINISH 检测使用了更宽泛的关键词匹配（"DONE"、"完成"、"COMPLETE"），与 SPEC 中的严格 `response.content.strip().upper().startswith("FINISH")` 有差异

**发现**：
1. SPEC 中对 FINISH 检测的描述不够精确——"FINISH" 关键词检测应该是开头精确匹配还是包含匹配需要明确
2. PLAN 中任务 3 的依赖描述正确（依赖 Task 2 的 LLMResponse）

**SPEC 修正**：在 SPEC 中补充了 FINISH 检测的精确语义。

## 4. SPEC 最终结构

SPEC.md 共 11 个章节：

| 章节 | 内容 | 页数估算 |
|------|------|---------|
| §1 | 问题陈述 | 目标用户、为什么值得做 |
| §2 | 用户故事 | 7 个 INVEST 用户故事 |
| §3 | 功能规约 | 主循环、工具系统、护栏（重点）、反馈、记忆、配置 |
| §4 | 非功能性需求 | 性能、安全、可观测性、可用性 |
| §5 | 系统架构 | 组件图、数据流图、外部依赖 |
| §6 | 数据模型 | 12 个核心实体 |
| §7 | 凭据与分发设计 | 威胁模型、存储方案、分发形态 |
| §8 | 技术选型与理由 | 9 项技术决策 |
| §9 | 验收标准 | 12 条验收条件 |
| §10 | 风险与未决问题 | 6 个风险及缓解措施 |
| §11 | 领域与机制设计 | 专属机制分析、重点维度说明 |

## 5. 三轮关键迭代对话节选

### 第一轮：架构选型追问

> **Agent**：你提到要做一个 Coding Agent 的 harness。在你想象中，这个 harness 的"主循环"大概长什么样？是一个简单的 while 循环，还是有更复杂的结构？

> **我**：我觉得就是一个 while 循环——调用 LLM、执行它返回的动作、把结果回灌、然后继续。

> **Agent**：这个描述接近 true，但缺少了几个关键环节。如果 LLM 返回的动作是危险的怎么办（比如 rm -rf /）？如果你跑了 100 轮 LLM 还没完成任务，谁来喊停？如果工具执行的结果包含了测试失败信息，谁来解析它并告诉 LLM "你需要修代码"？

> **我**：这些确实应该在循环里……所以不是简单的 while，而是每一步都要经过检查？

> **Agent**：对。建议你考虑一个"流水线"模式：上下文组装 → 调用 LLM → 解析动作 → 护栏检查 → 执行工具 → 收集反馈 → 停机判断 → 回到开头。每一步是一个独立的模块，可以单独测试。

**决策**：采纳顺序管道架构。这个追问让我意识到"Agent 的主循环"远不止"调用 LLM 然后执行"——护栏、反馈、停机都是 Harness 的硬逻辑，不能靠 LLM 自觉。

---

### 第二轮：护栏层次追问

> **Agent**：你说"危险操作要拦截"。但你打算怎么定义"危险"？所有危险都一视同仁地拦截吗？比如 `rm -rf /` 和 `pip install`，它们的危险程度一样吗？

> **我**：不一样……`rm -rf /` 应该直接拒绝，`pip install` 可能只需要问一下用户。

> **Agent**：很好，你已经有了"分层"的雏形。要不要考虑四级？FATAL 直接拒绝（如 rm -rf /、DROP TABLE），HIGH 需要用户确认（如 git push --force），MEDIUM 提示但可放行（如 sudo），LOW 自动放行（如 pytest）。每一层的判定逻辑用正则匹配——它是确定性的，不依赖 LLM 的判断。

> **我**：那如果用户不在电脑前呢？HIGH 级别的审批应该怎么处理？

> **Agent**：HITL（Human-in-the-Loop）审批 + 超时机制。给用户 60 秒，超时自动拒绝。这个超时必须是真的计时器，不能是"等用户输入"——因为 `input()` 在没有输入的时候永远不会返回。

**决策**：采纳四层护栏 + HITL 超时审批。这个追问让我从"拦截 vs 放行"的二元思维转向了"风险分级 + 状态机"的设计。

---

### 第三轮：冷启动验证后的修正

> **我**（向第二个 Agent 提供 SPEC + PLAN）：请根据这些文档实现 Action Parser。

> **第二个 Agent**：我有一个疑问——SPEC 中 §3.2 写了 4 个工具（read_file / write_file / shell / search），但 §9 验收标准中写的是"至少 3 个工具"，§11.2 也提到了"3 个基本工具"。我应该实现 3 个还是 4 个？

> **我**：（意识到 SPEC 内部矛盾）……4 个。

> **第二个 Agent**：另外，关于 FINISH 检测——SPEC 只说"检测 LLM 返回的 FINISH 关键词"。我理解为只要响应中包含 "FINISH" 就算。但 PLAN 中 Task 3 的测试代码写的是 `content.strip().upper().startswith("FINISH")`，这要求 FINISH 出现在开头。这两个是矛盾的吗？

**决策**：统一工具数量为 4 个，在 SPEC 中全局修正。明确 FINISH 检测为 `strip().upper().startswith("FINISH")` 的精确语义，并在 SPEC 中补充说明。

这轮验证暴露了 SPEC 的两类问题：**数字不一致**（4 vs 3）和**语义模糊**（包含匹配 vs 开头匹配）。如果不做冷启动验证，这些问题会留到实现阶段才发现，修正成本远高于规约阶段。

---

## 6. Brainstorming 技能反思

### 做得好的地方

1. **追问深度超出预期**。我在描述需求时经常使用模糊词汇（"安全的"、"灵活的"），Brainstorming 技能会追问"你说的安全具体指什么？什么操作算危险？谁来定义危险？"——这种追问迫使我把直觉变成精确的设计约束。

2. **分块签字确认**。Brainstorming 不会一次性抛出整个设计让我审阅，而是按模块（工具、护栏、反馈、配置……）逐块呈现并让我签字。这种节奏让每个决策都有了"停下来想一想"的空间，避免了"一口气看完整个设计然后全部点头"的认知过载。

3. **冷启动验证的建议**。Brainstorming 技能主动建议用不同 Agent 做冷启动验证——如果它不提，我大概率不会想到要做这一步。而这一步恰恰发现了 SPEC 中最关键的两处歧义。

### 让我不满的地方

1. **有时追问过多，拖慢节奏**。在技术选型阶段（Python vs TypeScript），Brainstorming 追问了太多细节（"你考虑过 TypeScript 的类型系统在处理 JSON 响应时的优势吗？"），但这些细节在项目初期并不关键——选 Python 纯粹是因为 LLM SDK 生态更成熟。过度的追问消耗了时间而没有产生更好的决策。

2. **生成的 SPEC 初稿有"模板感"**。Brainstorming 产出的 SPEC 初稿在格式上非常完整，但在某些章节（如 §4 非功能性需求）有明显的"填模板"感觉——"性能：响应时间 < 5s"、"可用性：99.9% uptime"这些数字不是从项目实际需求推导出来的，而是从某个模板复制过来的。这让我在后来自检时重新审视了这些数字的真实性。

3. **冷启动验证的"陌生 Agent"选择受限**。课程要求用"不同类型的 Agent"做冷启动验证，但我能接触到的 Agent 都是类似架构的（Claude Code、Codex CLI），它们之间的差异不够大。如果我能用 Gemini CLI 或完全不同的工具，暴露的 SPEC 歧义可能会更多。

---

## 7. 变更记录

| 日期 | 变更 |
|------|------|
| 2026-08-10 | 初始 brainstorming，产出 SPEC v1 |
| 2026-08-10 | 自检修正：工具数量、路径穿越、凭据清理、配置优先级 |
| 2026-08-10 | 冷启动验证：Action Parser 试实现，FINISH 语义澄清 |
| 2026-08-10 | 最终 SPEC 定稿，转入 writing-plans |
| 2026-08-12 | 补充三轮关键迭代对话节选 + Brainstorming 技能反思 |
