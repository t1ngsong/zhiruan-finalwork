# REFLECTION.md — 反思报告

> Coding Agent Harness · AI4SE 期末项目 · A 方向

---

## 一、从"使用 Agent"到"构建 Agent"

在这个项目之前，我使用过 Claude Code、Cursor 等 Coding Agent 完成编码任务。它们像黑盒一样工作——我输入任务，它们输出代码，中间发生了什么我不清楚。如果 Agent 执行了危险操作，我只能事后补救；如果 Agent 陷入循环，我只能强制停止。

这个项目让我从 Agent 的**使用者**变成了 Agent 的**构建者**。我亲手实现了主循环的每一步：上下文组装 → 调用 LLM → 解析动作 → 护栏拦截 → 分发工具 → 收集反馈 → 写入记忆 → 判断停机。当 Mock LLM 脚本驱动的 Agent 在测试中自主完成"运行测试 → 发现失败 → 读取文件 → 修复代码 → 再次测试通过"这个闭环时，我第一次真正理解了课程的核心命题：**Agent = LLM + Harness**。

LLM 只是一个"下一步做什么"的决策器。它不知道什么是危险，不知道自己是对是错，不知道何时该停。所有这些都需要 Harness 这层工程机制来保障。这个认识不是通过阅读获得的，是通过写代码——写 `GuardrailCoordinator`、写 `FeedbackCollector`、写 `StopChecker`——真正理解的。

## 二、护栏：代码比提示词更可靠

我选择治理护栏作为重点维度。这个选择证明了自己的价值。

护栏系统的四层架构——范围围栏 → 风险评分 → 致命拦截 → 人工审批——每一层都是确定性的代码，不是"提醒 LLM 注意安全"的提示词。在实现过程中，有几个时刻让我印象深刻：

**正则的精确性至关重要。** 最初的 `rm -rf` 正则写得太宽，连 `rm -rf /tmp` 都被标记为致命操作。收紧为只匹配 `rm -rf /` 和 `rm -rf /*` 后，又发现 `rm -r -f /`（分开写）仍然漏网。这个迭代过程让我意识到：**安全规则不能依赖模糊匹配，每一个变体都需要显式处理**。如果杀毒软件的病毒库这么写，机器早就被感染了。

**超时必须是真超时。** HITL 审批的第一版实现用 `input()` 等待用户输入——"因为用户不输入就等于超时"。评审指出这是假的超时实现，`input()` 会阻塞到天荒地老。正确的实现用 `threading.Event.wait(timeout=60)` 加 daemon 线程，真正做到了"60 秒后没有输入就自动拒绝"。这个教训的通用版本是：**如果你声称有超时机制，就必须真的能超时**。

**状态机必须真的转换。** 第一版 HITLGate 的 APPROVED/REJECTED/TIMEOUT 状态只存在于 docstring 里。代码在 `finally` 块中直接从 WAITING 跳到 IDLE，中间状态从来没有被设置过。修复后，`request_approval()` 在每个返回路径上都正确设置了终端状态。这让我理解了为什么课程强调"机制必须是代码，不能是提示词"——如果状态机不是真的，依赖它的代码就会做错误的假设。

## 三、反馈闭环：确定性是王道

反馈闭环的设计选择是"纯函数解析器"，而不是"让 LLM 检查代码"。`TestParser` 用正则匹配 `(\d+) passed` 和 `(\d+) failed`，`LintParser` 解析 `file:line: message` 格式，`TypeCheckParser` 解析 `file:line: error: message` 格式。每一个都是输入字符串、输出结构化数据的纯函数。

这个设计的价值在 Mock LLM 测试中体现得最明显：注入 `"3 passed, 1 failed"` 的 stdout 字符串，解析器产出 `TestResult(passed=3, failed=1)`，反馈回灌给 Agent，Agent 看到"有测试失败"的信息后触发修复行为——整个过程不需要真实 LLM，不需要真实 `pytest`，完全确定性地可测试。

相比之下，如果"反馈"是"请 LLM 检查输出是否包含错误"——这就是伪机制，因为 LLM 可能忽略错误、可能幻觉、可能在离线模式下不可用。**确定性 = 可验证性 = 可信性**。

## 四、Superpowers 与 SDD：工程纪律的脚手架

Superpowers 框架要求 brainstorming → writing-plans → worktree → SDD → TDD → code-review → finishing-branch 七步流程。在项目执行中，我体会到了这个流程的价值：

**Brainstorming 防止方向性错误。** 在开始写代码之前，我们花了大量时间讨论架构。顺序管道 vs 事件驱动、四层 vs 两层护栏、YAML vs TOML 配置——这些决策如果留到编码阶段再做，每次改变的成本都远高于 brainstorming 阶段。

**SDD 把"一个人做 N 件事"变成"N 个 Agent 各做一件事"。** 12 个 task，每个 task 派发给一个新鲜的 subagent，带着 task brief 进入、交出 commit 离开。10 个 task 一次通过，2 个需要修复轮次——这个成功率在软件开发中是相当高的。关键因素是：每个 task brief 都有精确的代码模板和测试用例，subagent 的任务是**转录和验证**，而不是**从零设计**。

**Code Review 是最后的安全网。** Task 5 的路径穿越漏洞（`str.startswith()`）、Task 6 的 HITL 假超时——这两个问题如果在没有审查的情况下合入，都会在生产环境中造成实际危害。审查者带着独立视角和全局约束来读 diff，发现了 implementer 在狭窄上下文中可能忽略的问题。

**但 SDD 也有代价。** 最大的代价是上下文碎片化——每个 subagent 只知道自己的 task，不知道前后 task 的设计决策。Task 1 创建的 `models.py` 到 Task 12 还在代码库里，因为没有一个 task 的 brief 说"删除未使用的 models.py"。这暴露了 SDD 的一个局限：**跨任务的关注点（如代码清理、全局命名一致性）需要显式地写进 brief 或留给最终审查**。

## 五、Mock LLM 测试的工程哲学

项目要求"移除真实 LLM 后，每个核心机制仍能用确定性单元测试验证"（§A.4(C)）。这不仅是测试策略，更是一种工程哲学。

Mock LLM 的威力在于它把"概率系统"变成了"确定性系统"。真实 LLM 每次可能返回不同的响应，但 Mock LLM 按预设脚本消费——第 1 轮返回"调用 shell rm -rf /"，第 2 轮返回"FINISH"——测试结果完全可复现。这意味着：

1. **回归测试是确定性的。** 如果今天 135 个测试通过了，明天改了代码后这 135 个测试仍然应该全部通过——不需要网络、不依赖 LLM 的状态、不受 API 限流影响。
2. **边界条件可以精确构造。** 工具调用参数 JSON 解析失败？注入 `'{invalid json'`。LLM 返回空响应？注入 `LLMResponse(content="")`。真实 LLM 永远不会返回这些边界情况，但 Harness 必须能处理。
3. **调试变得简单。** 测试失败时，Mock LLM 的脚本精确告诉你 Agent 在第几轮做了什么——不像真实 LLM，它的行为是可以"回放"的。

这让我重新思考了 AI 系统的测试策略：**任何包含 AI 组件的系统，如果其非 AI 组件（Harness）不能用 mock 独立验证，那么这个系统的可信性就是有问题的**。

## 六、如果重新做一个 Harness

如果要重新实现这个项目，有几件事我会做得不同：

**1. 更早引入集成测试。** Tasks 1-8 都是单元模块，各自功能正确。Task 9 把它们串起来时才发现接口之间的小摩擦（`execute_shell` 返回 dict 但 `ToolExecutor` 期望 string、`Config.workspace` 是 string 但 `ScopeFence` 期望 `Path`）。如果 Task 2 完成后就有一个端到端骨架测试，这些摩擦会更早暴露。

**2. 工具注册不应该有两套代码。** `register_all_tools()` 和 `build_agent()` 各自注册工具，配置不同，参数不同。这在最终审查中才被发现并修复。正确的做法是从一开始就让 `build_agent()` 调用 `register_all_tools()`。

**3. 护栏规则应该可配置。** 当前 8 个危险模式硬编码在 `patterns.py` 里。如果要添加新规则，需要改代码而非改配置。可配置的护栏规则（通过 `.agent.yaml` 的 `custom_patterns` 字段）已经框架就绪，但只有 Demo 3 测试了它。生产级的 Harness 应该让所有规则都可配置。

**4. 反馈信号应该更丰富。** 当前的 4 个解析器覆盖了测试/lint/类型检查，但还可以加：diff 解析器（检查修改的行数）、覆盖率解析器（检查覆盖率变化）、安全扫描解析器。多一个反馈信号，Agent 就多一个自我判断的依据。

**5. LLM 重试应该有断路器。** 当前遇到 LLM 调用失败时直接返回错误，缺乏指数退避重试。在生产环境中，API 暂时不可用是常见情况。

## 七、Agent = LLM + Harness 之外

在完成这个项目后，我对课程核心命题有了更深的理解：**当 LLM 能完成大部分编码工作时，工程师的价值在哪里？**

我的答案是三个层次：

**第一层：判断"做什么"。** LLM 能写出漂亮的代码，但它不知道这段代码是否应该存在。工程师的价值在于把模糊的需求转化为清晰的规约——这就是 brainstorming 和 SPEC 编写所做的工作。12 个 task brief 的精确性决定了 SDD 的成功率，而 brief 的精确性来源于人对需求的判断。

**第二层：判断"做对了吗"。** LLM 不知道自己的输出是否正确。工程师的价值在于设计确定性验证机制——测试、护栏、反馈闭环——这些机制不能是提示词，必须是代码。`StopChecker.should_stop()` 判断 Agent 是否可以停机的逻辑，是一段代码，不是一句"请自行判断任务是否完成"。

**第三层：构建可信的系统。** 单次 LLM 调用不可靠，但把 LLM 嵌入确定性工程框架后，系统整体可以可靠。这就是 Harness 的价值——它把不可靠的概率模型变成了可信的软件系统。这个洞察超越了 Coding Agent：任何 AI 应用，如果要做到生产级可靠，都需要类似的工程化封装。

## 八、结语

这个项目让我从"AI 工具的使用者"变成了"AI 系统的构建者"。我写的不只是一个课程项目——我写的是一个 AI Agent 的"操作系统"：调度器（主循环）、权限系统（护栏）、文件系统（工具分发）、监控（反馈闭环）、日志（记忆系统）。135 个测试证明了它的正确性，14 个有序的提交记录证明了它的工程过程。

但更重要的是，这个项目回答了课程最核心的那个问题：**当 AI 能写代码时，软件工程师的价值在哪里？**

**不在写代码本身——在设计和构建让 AI 能安全、可靠、正确地写代码的那一层工程系统。**

---

*2026-08-12 · 南京大学 · AI4SE*
