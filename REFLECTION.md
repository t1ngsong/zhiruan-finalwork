# REFLECTION.md — 反思报告

> Coding Agent Harness · AI4SE 期末项目 · A 方向
>
> *本文由学生撰写，使用 Claude Code 辅助结构梳理与文字润色。*

---

## 一、起点：我为什么要选这个题

说实话，刚开始读课程要求的时候我是有点懵的。"Agent = LLM + Harness"——这句话在课件里出现了很多次，考试也考了，但说实话在那之前我对它的理解也就停留在"背下来能得分"的水平。

真正让我决定选 A 方向的，是有一天我用 Claude Code 写一个 Python 脚本，中间 Agent 执行了一条我不认识的 shell 命令，我本能地按了 Ctrl+C。后来回头看日志，发现那只是一个 `mkdir -p`。但这个瞬间让我意识到一件事：**我作为用户，对 Agent 的行为是完全盲目的**。它替我做了决定，但我既不知道它为什么做这个决定，也不知道它有没有在背后做了我不乐意的事。

选 A 方向就是出于这个困惑：那个让我不安的黑盒里面，到底应该装什么？把 LLM 封装成一个可信的工具，中间需要哪些工程机制？这门课既然说答案是"Harness"，那我自己来写一个。

---

## 二、哪些 Superpowers 技能发挥了最大作用、哪些"形式大于实质"？

### 真正改变了我做事方式的

**Brainstorming 是我用过的最好的"想法照妖镜"。** 以前我写课程项目都是脑子里有个大概就动手写代码，卡住了再改。Brainstorming 完全不是这个路子——你说一句"大概这样"，它不会让你混过去。我记得最清楚的一次是我随口说"大概 3 到 4 个工具吧"，它没有放过我，追问了一句："到底是 3 个还是 4 个？4 个的话，第 4 个是谁？3 个的话，你确定能覆盖 Coding Agent 的全部操作场景？"我当时真的愣住了——因为我说"3 到 4 个"的时候，我自己也没想清楚到底是 3 还是 4。

我花了十分钟认真梳理，最后定下来 read_file / write_file / shell / search 四个。这个在当时的我看来微不足道的"十分钟"，后来成了整份 SPEC 里最值钱的决策之一——冷启动验证时，第二个 Agent 翻完 SPEC 后问的第一个问题就是："§3.2 说 4 个工具，§9 说 3 个，到底按哪个来？"如果 brainstorming 当时没逼我较这个真，后面 12 个 subagent 各自按自己的理解去实现——那种混乱我真的不敢往下想。

**SDD 让我第一次体验了"像导演一样工作"。** 12 个 task，12 个 subagent，每个都是一个新鲜的脑子，不带着上一个 task 的包袱。我把 task brief 写得越精确，结果就越好——Task 4（Action Parser）的 brief 我甚至写了 FINISH 检测的精确前后 20 字符的规则，subagent 一字不差地实现了，一次通过。那种感觉不像我在写代码，像我在给一个特别认真但完全没有创造力的工程师下指令。说实话有点上瘾。

**TDD 在 AI 协作下是一个我之前完全没意识到的武器。** 不是因为它保证了代码质量——这个我已经在学校里听过一万遍了。而是因为它解决了 AI 协作里一个特别恶心的问题：**subagent 说"DONE"的时候，你怎么知道是真的完成了还是它以为完成了？** 红色测试 = 没完成，绿色测试 = 至少通过了这些场景。这个二进制信号比任何"我觉得差不多了"都可靠。135 个绿灯对我来说不只是"证明代码对了"，更是**证明 12 个不同的 subagent 各自按照我的意图实现了，而且它们之间没有互相破坏**。那种安全感，不用 TDD 你根本体会不到。

### 让我觉得"这个环节可以跳过去"的

**Finishing-a-development-branch** 的设计假设是你在一个团队里——有 PR review、有多人审批、有合并策略需要讨论。但我是一个人。整个过程里，这个技能唯一实际帮到我的就是最后的 `git worktree remove && git worktree prune`，清掉了残留的工作区。其余的东西——三选一菜单、确认 base branch——在单人项目里纯属走过场。这不是说技能写得不好，是说它的使用场景和我这个项目不匹配。

**Using-git-worktrees** 在 Claude Code 的原生 EnterWorktree 功能面前显得很尴尬。平台已经替你管好了工作区的创建、切换、销毁，你又手动去检测一遍"我是不是在 worktree 里"——这不叫严谨，叫重复劳动。如果我用的是一个不带 worktree 管理功能的工具，这个技能才真正值钱。

---

## 三、TDD 强制在 AI 协作下是阻碍还是放大器？

我以前对 TDD 的态度说实话挺功利的——考试要考，项目要求有测试，那就写呗。但这个项目改变了我对 TDD 的看法。

第一次被 TDD 震撼是在 Task 5 的修复轮次。Subagent 提交的代码用 `str.startswith()` 做路径比较来防止穿越，reviewer 指出 `workspace="/tmp/ws"` 时 `/tmp/wsfoo/evil` 会被认为是安全的。我先写了 9 个新的失败测试——`test_rejects_path_traversal`、`test_rejects_prefix_collision`、`test_rejects_absolute_path` 等等——每个都精确描述了一个攻击向量。然后让 subagent 把这些测试从红变绿。**如果你反过来做——先修代码再补测试——你永远无法确定修复是否真的覆盖了所有你想到的场景**。红色测试就像一份待签字的合约：subagent 必须逐条满足，不能含糊。

更关键的是 Mock LLM 让 TDD 变得特别轻。所有测试用 `MockLLMAdapter` 注入预设脚本，不调真实 API，不依赖网络状态。135 个测试跑完不到 2 秒。如果每次跑测试都要烧 DeepSeek 的额度 + 等网络延迟，TDD 的红绿循环会变成一种折磨。Mock 把 TDD 从"理论上正确但实践中痛苦"变成了"正确且舒服"。

但 TDD 也不是万能的。它验证的是"你写出来的行为"，不是"你没写出来的行为"。Task 1 创建的 `models.py` 文件从第一个 commit 存在到最终审查才被清理，中间 11 个 commit 没有任何测试失败——因为这个文件从来没有被引用过，自然也不会有测试覆盖到它。**TDD 保证你实现了你想要的，不保证你没有留下你不想要的**。这是 TDD 的盲区，也是为什么最终的全分支审查不可替代。

---

## 四、subagent-driven 工作流让智能体能自主运行多久而不偏离主题？什么样的 task 颗粒度最优？

SDD 最打动我的，不是它有多快，而是它**不累**。我写代码写着写着会烦，会走神，会想着"这轮差不多就行了吧"。Subagent 不会。你给它一个精确的 brief，它会一板一眼地把每一步都做到，不会因为倦怠而跳过测试或者偷懒少写两个边界条件。

在我这个项目里，一个 subagent 的自主运行窗口大概是 15 到 30 分钟。这个时间刚好是一个 task 的完整周期：读 brief → 理解接口 → 写实现 → 写测试 → 提交。超过 30 分钟的 task（比如跨 5 个模块的 Task 6），偏离的概率显著上升。Task 6 是唯一一个需要 Sonnet 模型加修复轮次的 task，它的 brief 相对开放，给了 subagent 太多"自由发挥"的空间——结果就是 `input()` = 超时这种"看似聪明实则愚蠢"的假设。

关于 task 颗粒度，我的经验是一句话：**一个 task 应该是一个人可以在 30 分钟内完全理解并验证的范围**。具体到代码上就是 2-4 个源文件加 1 个测试文件，5-20 个测试用例。比这个小（比如光写一个 dataclass）就是杀鸡用牛刀——派发 subagent 的开销比实际编码还大。比这个大（比如 Task 6 的五模块协调）就是撒把——subagent 的注意力宽度不够覆盖所有跨模块约束。

---

## 五、SPEC / PLAN 质量如何影响实现质量？举一个"规约不清导致 subagent 偏离"的具体案例

说一个让我至今后怕的故事。

在写 SPEC v1 的时候，我同时提到了工具数量。§3.2 功能规约里写了 4 个工具，§9 验收标准里写了"至少 3 个工具"。我当时脑子里想的是"4 个当然是至少 3 个，这有什么矛盾的"。但在冷启动验证的时候，第二个 Agent——一个从来没有和我对话过的 Agent——读到这两处之后停下来了。它问我："§3.2 说 4 个，§9 和 §11.2 说 3 个。我应该按哪个来？"

我愣住了。

如果冷启动验证没有暴露这个矛盾，Task 5（工具系统）的 subagent 可能实现 4 个工具，Task 12（机制演示）的 subagent 可能按照"至少 3 个"的理解只测试 3 个。到合并的时候才发现接口对不上。那时候 12 个 task 都做完了，再回头改——我甚至不敢想那种痛苦。

还有一个更微妙的例子。SPEC 初版写 FINISH 检测是"检测 LLM 返回的 FINISH 关键词"。我心想这够清楚了。结果冷启动 Agent 把它理解成了"只要响应里包含 FINISH 就算"——把 "DONE"、"完成" 都纳入了 FINISH 检测。但我要的其实是 `content.strip().upper().startswith("FINISH")`——只有以 FINISH 开头才算。**"检测关键词"这四个字背后藏着至少两种完全不同的实现**，而我在写 SPEC 的时候完全没意识到这个歧义。

这两个教训让我真正理解了那句话：**共享的隐性上下文是规约质量的最大敌人**。你和主开发 Agent 在 brainstorming 阶段积累了大量"你懂的"默契，这些默契在 SPEC 文本里一个字都没有。但新的 subagent 没有这些默契——它只有 SPEC 上的文字。文字有多精准，结果就有多靠谱。

---

## 六、你最有效的 prompt / context 策略是什么、为什么有效？

回顾整个项目，我觉得最有效的三个策略是：

**第一，把 task brief 写成合约而不是建议。** 坏的 brief："实现一个工具系统，支持读文件和执行命令"。好的 brief："实现 ToolExecutor.execute_tool(tool_name, args) → ToolResult，handler 签名见附 A，危险路径必须用 Path.is_relative_to() 做判断，禁止使用 str.startswith()"。好的 brief 不给你选择——它告诉你精确的输入输出类型、禁止使用的模式、必须覆盖的测试。当 brief 精确到这种程度的时候，subagent 与其说是在"设计"，不如说是在"转录"——而转录几乎是不会出错的。Task 2 到 4、7 到 8、10 到 12 都用的是这种合约式 brief，全部一次通过。Task 5 和 6 的 brief 相对开放，都进了修复轮次。这个相关性不是巧合。

**第二，Haiku 能干的事别用 Sonnet。** 机械性的 task（脚手架、解析器、CLI 入口、配置文件）用便宜的 Haiku，需要跨文件判断的集成性 task（主循环组装、护栏协调器、全分支审查）用贵的 Sonnet。12 个 task 只有 2 个用了 Sonnet，它们恰好是复杂度最高的两个。模型选择不是越强越好——是**匹配复杂度**。用 Sonnet 写 dataclass 是浪费，用 Haiku 设计状态机是赌博。

**第三，修 bug 的时候尽量保持同一个 implementer。** SDD 的修复循环设计是 Rounds 1-3 继续用原 implementer。我的实践验证了这个设计：小范围修复，原 implementer 对代码的上下文记忆比重新读 diff 高效太多了。让一个新 subagent 来修别人的代码，它首先要花时间理解"这段代码为什么写成这样"——而原 implementer 从一开始就知道。

---

## 七、凭据与分发这两条工程要求，迫使你想清楚了哪些原本会忽略的问题？

凭据管理不是技术难题，是纪律难题。加密存储、密钥派生、隐藏输入——这些技术上都不复杂，随便一个 cryptography 库都能搞定。真正难的是**把"Key 可能从哪里泄露"这个问题想全了**。

我之前写个人项目的时候，Key 就是往 `.env` 文件一放，靠 `.gitignore` 保证不提交。但这个项目逼我走了一遍完整的威胁链条：源码里不能有 Key ✅，`.gitignore` 排除了 `.env` ✅，那终端历史呢？如果你用 `export DEEPSEEK_KEY=xxx` 的方式传 Key，它会留在你的 shell history 里，随 `.bash_history` 一起同步到 GitHub（很多人不知道这一点）。日志呢？Agent 的运行输出如果包含 Key——比如你打印了 `f"Using API Key: {key}"` 来调试——Key 就进了终端输出。Docker 镜像呢？如果 `docker build` 的时候用 `--build-arg API_KEY=xxx`，Key 就进了镜像层，任何人拿到镜像都可以 `docker history` 看到。

**安全不是做了一件事就安全了，是在每一个可能的泄露路径上都放了一个拦截点。** 凭据管理这条要求逼迫我画出了"Key 的完整生命周期"——录入（`getpass` 隐藏）、存储（AES-256-GCM + PBKDF2HMAC 60万次迭代）、使用（内存中，不写文件）、传输（HTTPS，不缓存日志）——每一步都必须是显式的安全设计。

Docker 分发也逼我想清楚了一个很实际的问题：别人拿到我的项目怎么跑。不是"把代码发给他就行"——他得有 Python 3.11、得装依赖、得配 Key、得知道工作区怎么挂载。Dockerfile + README 的 Quick Start 就是在回答这个问题：从零到可运行的最小步骤。我当时写 Dockerfile COPY 顺序的时候犯了个错——先 `pip install` 再 `COPY src/`——导致 pip 找不到包。这恰好说明了一个道理：**分发方案不是"设计好了就对的"，是要真的能在干净环境里跑一遍。** CI 的 build-docker job 就是做这件事的。

---

## 八、如果重做你会改变什么？

**最开始就做一个骨架集成测试。** Tasks 1 到 8 各自美好，到了 Task 9 把它们拼起来的时候才发现三个接口摩擦：`execute_shell` 返回 dict 但 `ToolExecutor` 要 string、`Config.workspace` 是 string 但 `ScopeFence` 要 Path、工具注册有两套重复的代码。如果 Task 2 完成之后我就用 Mock LLM 跑一轮"空循环"——不需要真正的功能，只需要所有组件能串在一起——这些摩擦会在第一时间暴露。**发现接口摩擦的成本，第 2 天发现 vs 第 9 天发现，差的不是 7 天的耐心，是 7 天里已经在错误接口上建起来的一整套测试。**

**从一开始就统一工具注册。** `register_all_tools()` 和 `build_agent()` 各有一套注册逻辑，这个问题到最终审查才发现。SDD 有个结构性盲区：**同一段逻辑在两个地方复现了，但没有任何一个 task brief 会覆盖"发现并消除重复"这件事**。跨 task 的代码清理和统一，要么写进全局约束让每个 reviewer 都关注，要么在最终审查中专设一轮"去重检查"。

**护栏规则从一开始就做成可配置的。** 现在 8 个危险模式硬编码在 `patterns.py` 里，加了 Demo 3 之后才验证了 `custom_patterns` 字段的功能。但如果让我重来，我会从一开始就让所有规则——包括内置的 8 个——都通过 YAML 配置。这不是技术挑战，是设计先见性的问题。

---

## 九、你对 Superpowers 这套方法论的批判——它假设了什么，这些假设在你的项目里成立吗？

Superpowers 的核心赌注是：**如果规约足够清晰，subagent 就能独立正确地实现。** 它假设开发可以分解为"设计 → 派发 → 验收"的线性流水线。

这个假设在我的项目里大部分时候成立。Task 1 到 4、7、8、10 到 12 的规约都可以精确到函数签名和测试用例级别，subagent 确实一次性交付了正确的结果。这个成功比例（10/12）让我相信这套方法论在**规约可精确化、模块可独立化**的项目中是非常高效的。

但它也在两个地方遇到了麻烦：

**第一，安全边界的规约是穷举不完的。** `str.startswith()` 在什么情况下会被绕过？这个问题在写 SPEC 的时候我完全没想过。不是我不认真——是我缺乏"攻击者思维"。规约写得再详细，也会在安全攻防这种需要对抗性思维的领域露出盲区。Superpowers 的 reviewer 如果能带着特定领域的审查框架（而不仅仅是"spec vs code"对照）来读代码，这些问题会被更早发现。

**第二，跨 task 的质量问题会从审查的指缝里漏掉。** `models.py` 这个 122 行的死文件活过了 11 个 commit。因为 task-scoped reviewer 只看当前 diff，不关心"这个文件整个项目里有没有人 import"。全局死代码、重复逻辑、命名不一致——这些都是 task-scoped review 的系统性盲区，必须由全分支审查来兜底，而全分支审查在 SDD 的流程里只有一次。

**Superpowers 在"规约清晰 + 模块独立"的项目中是一台高效的流水线；在"安全敏感 + 跨模块约束多"的项目中，它需要一个外挂的、跨 task 的审查轮次来弥补盲区。** 这不是说 Superpowers 不好——它提供的框架比没有框架强太多了。但它确实假设了"设计阶段可以穷举所有重要约束"，而这个假设在你不是该领域的专家时——比如我第一次设计 Agent 护栏——是不成立的。你不知道什么是你不知道的。

---

## 写在最后

做这个项目之前，我以为 AI 编程最难的部分是让 LLM 写出对的代码。做完之后我意识到，**让 LLM 写出对的代码一点都不难——难的是在它写错的时候、写出危险代码的时候、写到一半不知道自己在干什么的时候，你的系统能不能兜住。**

这就是 Harness 的意义。LLM 是大脑，Harness 是小脑、免疫系统、前庭平衡——所有那些不需要思考但必须正确的底层机制。135 个测试、四层护栏、反馈闭环、凭据加密——这些东西都不是为了"让 Agent 更聪明"，而是为了"让 Agent 在不够聪明的时候也不至于造成灾难"。

课程最核心的那个问题——"当 AI 能写代码时，工程师的价值在哪里"——做完这个项目，我有了一个自己亲手试出来的回答：**不在写代码本身。在设计让代码被安全、可靠、正确地写出来的那台机器。** LLM 替掉的不是工程师，是没有工程规范的手工作坊。HITL 审批、风险评分、反馈闭环、凭据全生命周期管理——这些不是"代码"，但这些才是 AI 时代一个工程师真正的护城河。

---

*2026-08-12 · 南京大学 · AI4SE*

*标注：本文核心观点、个人经历、全部案例由学生提供，Claude Code 辅助结构梳理与文字润色。*
